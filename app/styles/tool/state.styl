/**
 * Unicode icons
 *
 * Describe the appearance of a module or object in various states
 * States are usually triggered (toggled) via javascript
 *
 * @link https://smacss.com/book/type-state
 */

// loading animation (css spinner)
.is-loading {
    position: relative;

    &:after {
        position: absolute;
        top: 50%;
        left: 50%;
        z-index: 10;
        margin: -10px 0 0 -10px;
        border: 5px solid rgba(0, 0, 0, 0.1);
        border-top: 5px solid rgba(0, 0, 0, 0.5);
        border-radius: 100%;
        height: 20px;
        width: 20px;
        animation: rotation .6s infinite linear;
        content: '';
    }
}

// spinner animation
@keyframes rotation {
    from { transform: rotate(0deg); }
    to { transform: rotate(359deg); }
}

// greyed out and unclickable
.is-disabled {
    opacity: 0.30;
    pointer-events: none;
}

.is-hidden {
    display: none !important;
}

.is-invisible {
    visibility: hidden !important;
}

.is-visible {
    visibility: visible !important;
}
